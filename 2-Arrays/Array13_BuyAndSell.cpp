#include <bits/stdc++.h>
using namespace std;

int buyAndSell(int *arr, int n)
{
}

int main()
{
    int arr[1000];

    int n;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    cout << buyAndSell(arr, n);

    return 0;
}